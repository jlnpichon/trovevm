# Trove CLI

The `trove-cli` crate provides a command-line interface to TroveVM.  
It can parse, compile, run, and evaluate TroveLang source files.

## Commands

- `trove parse <file>` → prints the AST
- `trove compile <file>` → outputs bytecode
- `trove run <file>` → executes code in the VM
